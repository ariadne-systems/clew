node_modules/
dist/
coverage/
*.log
.DS_Store

# Staging temporaries from atomic writes (removed on success; stray after a crash)
*.tmp

# Scan output: regenerated by `clew scan`, derived from the code (state.json stays tracked).
.ariadne/locations.json

# Coverage report: regenerated by `clew check`/`clew coverage`, derived from the corpus.
.ariadne/coverage.json

# Editors
.idea/

# Claude Code (local, per-user)
.claude/settings.local.json

# Internal planning notes (not published)
notes/
